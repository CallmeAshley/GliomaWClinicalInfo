# data_dir: "/mai_nas/BYS/brain_metastasis/preprocessed/SEV_2d_50/"
batch_size: 64 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
workers: 8
pin_memory: True
seq_comb: "4seq"
aug_type: "heavy"

slice_percentile: 75
training_size: 224

k_fold: 5 # number of folds in cross validation
fold_num: 0

class_balance_load: False
